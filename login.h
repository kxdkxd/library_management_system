#ifndef LOGIN_H
#define LOGIN_H

#include <QMainWindow>
#include <QWidget>

class login : public QMainWindow
{
    Q_OBJECT
public:
    explicit login(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // LOGIN_H